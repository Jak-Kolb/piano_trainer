# Launch Keys

```bash
cd /Users/jakkolb/Desktop/Coding/GitHub/piano_trainer
npm start
```

Or double-click `scripts/Keys.command` (macOS). First run needs `npm install` once.

Copy `scripts/Keys.command` to your Desktop if you want a one-click launcher.
